# Rubik Robot API dependencies
#
# Core API
flask>=2.0

# Raspberry Pi hardware
RPi.GPIO>=0.7
# Camera uses picamera2 (libcamera stack), which must be installed via the
# system package manager -- it is NOT a pip dependency:
#     sudo apt install -y python3-picamera2
# (Required on Raspberry Pi OS Bookworm; the old `picamera` library only
#  worked on Bullseye and earlier.)

# Image processing
Pillow>=8.0

# Cube solving (used by the external solver, not this API directly,
# but useful for validating scanned cube states)
kociemba>=1.2

# PCA9685 driver (only needed for --driver pca9685)
Adafruit-PCA9685>=1.0

# OLED display (only needed for --driver pca9685)
luma.oled>=3.0
luma.core>=2.0
